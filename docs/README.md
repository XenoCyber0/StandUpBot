# docs/

Assets for the project README.

## TODO(RELEASE): record the demo GIF

`README.md` references [`demo.gif`](./demo.gif). **That file does not exist yet**
— it is a placeholder that must be replaced with a real screen recording before
the repo is made public.

Suggested recording (30–60s):

1. On a feature branch with a few commits, run `standupbot pr-description` and
   show the generated title / Summary / Changes / Testing output.
2. Run `standupbot pr-description --write` (or open a PR) to show it filling the
   PR description.

Recording tools: [ScreenToGif](https://www.screentogif.com/) (Windows),
[Kap](https://getkap.co/) (macOS), or `asciinema` + `agg` for a terminal-cast GIF.

Save the result as `docs/demo.gif` (keep the filename so the README image link
keeps working), then delete this note's TODO line.
